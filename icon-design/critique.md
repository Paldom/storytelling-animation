# Icon critique (advisory ranking; the owner picks)

Renders: `icon-design/renders/<candidate>/preview.html` (512 / 64 / 32 / 16 px, qlmanage).
Scores 1–5 per axis, grounded in the renders.

| Candidate | Reads as subject | Mass (ink @64) | 16 px silhouette | Gestalt | Stroke | Centering | Contrast | Verdict |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2-speaking-frame | 5 — a frame that speaks; stranger says "a screen with a tail" | 5 (28%) | 4 — frame, tail and inner card all survive; the dark inset thins to a 1 px ring | 5 closure, one device | 5 (fills only) | 4 — tail pulls weight down 8 units, acceptable | 5 | Strongest at every size; the accent card reads as a slide. |
| 3-rising-card | 4 — a text card over its trail; could be read as a document | 5 (26%) | 3 — the two trail bars fuse with the card into one lump; the accent bar survives | 4 figure-ground | 5 | 5 | 5 | Clean but generic at 16 px (a "note" glyph). |
| 1-beat-sheet | 4 — three beats climbing; "progress squares" | 3 (16%, thin) | 3 — three dots on a diagonal; the accent square holds | 5 continuity | 5 | 4 — diagonal weight sits low-left | 5 | Most distinctive silhouette, weakest mass; reads as "steps/levels" to a stranger. |

Distinctiveness notes (not used to drop anything): 2 sits near "comment" bubbles but
the filled inner card and square shoulders keep it off the chat glyph; 3 is close to
document/note UI glyphs; 1 is closest to "stairs/levels" progress marks and to nothing
in the video-tool category.

Recommendation: **2-speaking-frame** (the frame is the narration: on-message for a
muted feed, and the only candidate that survives 16 px intact). Runner-up:
1-beat-sheet if the owner prefers the story-beats metaphor over the frame.

Provisional: `assets/icon.svg` is a copy of candidate 2 so the README validates; the
owner's pick replaces it (copy any candidate over `assets/icon.svg`). No
`icon-design/icon.svg` was written: that file marks human approval.
